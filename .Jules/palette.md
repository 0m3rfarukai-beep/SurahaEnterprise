## 2026-07-15 - Added Loading State to ContactForm Submit Button\n**Learning:** The project relies heavily on visual feedback through Lucide React icons, but some async operations lack proper loading states, causing potential user confusion during form submissions.\n**Action:** Replaced static icons with `Loader2` + `animate-spin` during loading states on submit buttons, and ensured both loading and static icons have `aria-hidden="true"` to prevent redundant screen reader announcements.
