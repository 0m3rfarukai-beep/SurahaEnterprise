## 2024-06-20 - Adding Loading States to Async Buttons\n**Learning:** Implementing explicit loading states (like spinners) on submit buttons during async operations provides immediate, critical feedback to the user, preventing multiple submissions and confusion.\n**Action:** Always consider replacing static icons with spinning loaders (e.g., `Loader2` from `lucide-react`) and updating button text when an action is actively processing.
