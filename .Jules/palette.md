## 2024-06-18 - Loading States for Async Actions\n**Learning:** Implementing visual feedback (spinners) during async operations like form submissions significantly improves perceived responsiveness and reassures users the app isn't frozen.\n**Action:** Replaced static icon with `Loader2` component from `lucide-react` combined with Tailwind `animate-spin` during `loading` state in ContactForm.jsx.
