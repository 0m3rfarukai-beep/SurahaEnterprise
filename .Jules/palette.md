## 2024-05-19 - Adding loading states to actionable forms
**Learning:** Adding a loading spinner to form submission buttons significantly improves the UX by preventing user frustration and duplicate submissions during network requests.
**Action:** Always include a visual loading state (like `Loader2` from `lucide-react` with `animate-spin`) combined with text changes for any form or critical async operation.
