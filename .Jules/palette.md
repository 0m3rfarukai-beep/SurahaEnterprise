## 2026-06-24 - Adding loading spinner to async submit button
**Learning:** The established pattern for async action loading states in this application pairs `Loader2` from `lucide-react` with Tailwind's `animate-spin` class.
**Action:** Reused the `Loader2` icon with `animate-spin` inline instead of creating a custom SVG or CSS animation, keeping modifications below 50 lines and consistent with existing patterns.
