## 2026-08-06 - Added Loader2 spinner to Contact Form
**Learning:** The project relies on the `Loader2` component from `lucide-react` alongside Tailwind's `animate-spin` class for loading states, and applying `aria-hidden='true'` to supplementary icons prevents redundant announcements.
**Action:** Applied this pattern to the submit button in `ContactForm.jsx` for better user feedback and accessibility during asynchronous operations.
