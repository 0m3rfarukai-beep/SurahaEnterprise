## 2024-05-15 - Loader2 standard
**Learning:** Loading states for async actions should use the `Loader2` component from `lucide-react` with the Tailwind `animate-spin` class per project memory.
**Action:** Implemented this pattern on the ContactForm submit button to improve user feedback during async submission.
