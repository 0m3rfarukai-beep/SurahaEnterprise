## 2024-03-05 - Loading state for Contact Form Submit Button
**Learning:** The project convention is to use `Loader2` from `lucide-react` with `animate-spin` for loading states. Adding `aria-hidden="true"` to supplementary icons within buttons prevents redundant screen reader announcements.
**Action:** Applied this pattern to the ContactForm component to provide visual feedback during async submission and improved accessibility.
